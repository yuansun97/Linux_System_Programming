/**
 * Critical Concurrency
 * CS 241 - Spring 2020
 */
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>

#include "semamore.h"

int main(int argc, char **argv) {
    printf("Add tests in semamore_tests.c\n");
    // Semamore *s = malloc(sizeof(Semamore));
    // semm_init(&s, 2, 5);

    // pthread_create();

    return 0;
}
